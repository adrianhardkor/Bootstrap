---
- name: Run local expect script via shell
  hosts: all
  gather_facts: no

  tasks:
  - name: Run local expect script via shell
    command: expect ./src/ts_show.tcl {{ lx_ip }}
    register: response
  - name: "Print result"
    debug:
      var: response

